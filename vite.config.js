import { defineConfig } from "vite";

export default defineConfig({
  server: {
    port: 6200, // Change this to any port number you want (e.g., 3000, 4000, 8080)
    open: true, // Optional: Automatically opens the project in your browser when the server starts
  },
});
